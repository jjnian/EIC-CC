# 推演深化功能使用说明

> 编写日期：2026-05-23
> 适用版本：包含 P1-7 / P1-8 / P1-10 三个推演深化功能
> 相关章节：README §6 因果推演、ROADMAP-v2 Phase 1

本文档介绍三个新增的推演体验增强功能，建议先熟悉 README 第六章「因果推演」再阅读本指南。

---

## 目录

- [一、概率约束（P1-10）](#一概率约束p1-10)
- [二、原始 prompt 查看（P1-8）](#二原始-prompt-查看p1-8)
- [三、节点"为什么会发生？"详细解释（P1-7）](#三节点为什么会发生详细解释p1-7)
- [四、典型工作流串联示例](#四典型工作流串联示例)
- [五、常见问题](#五常见问题)

---

## 一、概率约束（P1-10）

### 1.1 它解决什么问题？

传统的 `force` / `block` 是**二元**约束——要么 100% 必然，要么 100% 禁止。但现实中很多假设是**有可能性的**：

- "供应商断货的概率大概 70%"
- "新规生效的可能性只有 30%"
- "管理层批准 B 方案的把握大约 60%"

这种情况下，`force` 会高估冲击、`block` 会完全忽略，都不准确。**概率约束**允许你给某个节点指定一个 0~1 的**先验概率**，推演引擎会把它作为下游联合概率的入口初值，并提示 LLM 用贝叶斯方式更新置信度。

### 1.2 如何使用

#### 添加概率约束

1. 打开推演对话框 `PredictDialog`，展开「What-if 约束」面板
2. 在搜索框输入目标节点名称
3. 候选项右侧除了原有的「禁止 / 必然」两个按钮，新增**紫色「概率」**按钮
4. 点击「概率」→ 约束行出现：

```
[概率]  「主供应商断货」  ━━━●━━━━  0.70  [×]
            ↑ 节点标签         滑块      数值
```

5. 拖动滑块或观察数值（0.00 ~ 1.00，步长 0.05）调整先验

#### 三态循环切换

约束行最左侧的模式按钮支持点击循环切换：

```
必然(蓝)  →  禁止(红)  →  概率(紫)  →  必然(蓝)  →  ...
```

切换到「概率」时自动设默认值 0.5，再切到「必然/禁止」时概率值会被清除。

#### 视觉编码

| 模式 | 按钮颜色 | 含义 |
|---|---|---|
| **必然 (force)** | 蓝 `#63b3ed` | 该节点 100% 发生 |
| **禁止 (block)** | 红 `#ff8a8a` | 该节点 0% 发生，下游被剪枝 |
| **概率 (probability)** | 紫 `#d4a8ff` | 先验值 ∈ [0, 1]，作为推演入口初值 |

### 1.3 约束作用范围

**概率约束只能作用于现有图谱节点**（`source` 为 `derived` / `inferred` / `preset` 的节点）。对推演节点（`source=predicted`，id 以 `p_` 开头）施加概率约束没有意义——它们本来就由本次推演动态生成。UI 会自动**禁用候选列表中预测节点的「概率」按钮**，hover 显示提示「概率约束仅适用于现有图谱节点」。

`force` / `block` 不受此限制。

### 1.4 后端处理逻辑

提交推演后：

1. **prompt 注入**：约束摘要新增「概率」分支，例如：
   ```
   - 概率: 主供应商断货(n_12) 先验概率 = 0.70 (probability)。
     请把先验作为初值，结合上下游证据用贝叶斯式更新；返回的 confidence 应反映综合后验。
   ```
2. **联合概率初值替换**：`PredictionOrchestrator.buildStep()` 计算 `effectiveProbability` 时，把先验值作为该现有节点的入口初值（替代默认的 1.0），影响下游所有引用它的预测步骤的 `effProb` 和 `cumulativeCredibility`

简而言之：**给节点 X 设 0.7 先验 → X 的所有下游预测节点的 effectiveProbability 自动被打 0.7 的折扣**。

### 1.5 与冲突检测的关系

现有的「force ↔ block 直接因果边冲突告警」**不会扫描概率约束**——概率本身就是中间态，与必然/禁止不直接冲突。如果你需要细粒度地表达"该节点很可能不会发生"，建议用 `probability=0.05~0.2` 而不是 `block`。

---

## 二、原始 prompt 查看（P1-8）

### 2.1 它解决什么问题？

推演结果"诡异"时，最有用的排错信息往往是：**到底发给 LLM 的完整 prompt 长什么样**。以前这些信息只能在后端日志里捞，本功能把它直接搬到 UI 上。

### 2.2 如何使用

1. 等待一次推演**完成**（状态变为「已完成 / 出错 / 已停止」均可，运行中不可用）
2. 在对话面板中找到那条「⚡ 推演」消息（金色边框）
3. 消息头部右上角（在「停止」按钮原位置）出现 **「📜 prompt」** 按钮
4. 点击 → 弹出模态对话框，等宽字体展示完整文本：

```
=== SYSTEM ===
你是一个基于本体图谱的前向推演 (Forward Simulation) 引擎。
给定现有图谱（节点、边、规则）和一个或多个起点节点 (seeds)，请沿因果链向前预测...
（system 指令 + JSON schema）

=== USER ===
当前本体图谱：
节点 (id | label | type):
  n_1 | 主供应商断货 | event
  n_2 | 库存吃紧 | event
  ...

起点节点 (seeds)：
  - n_1 (主供应商断货)

What-if 约束（必须严格遵守）：
  - 概率: n_1(主供应商断货) 先验概率 = 0.70 (probability)。...

额外场景说明：假设主供应商断货 3 天，叠加旺季订单激增 30%

请向前推演 4 步，严格按 schema 输出 JSON。
```

5. 底部按钮：
   - **「复制全文」**：把整段 system + user 文本写入剪贴板
   - **「关闭」**：关闭对话框

### 2.3 限制

- **旧分支不带 prompt 快照**：在本功能上线**之前**生成的分支，`rawPrompt` 字段为空，弹窗会显示：
  > 该分支创建于早期版本，未保存原始 prompt。
- **网络失败**：API 返回非 2xx 时弹窗显示「无法加载原始 prompt：HTTP xxx」

### 2.4 典型用途

| 你想确认… | 在 prompt 中查看… |
|---|---|
| 上下文是否被截断了 | 是否有「(为控制 LLM context 已截断 N 个节点...)」段 |
| 约束是不是被正确编码 | 「What-if 约束」段的具体文本 |
| 自然语言 prompt 是否被采纳 | 「额外场景说明」段 |
| LLM 拿到的图谱摘要长什么样 | 「当前本体图谱」段的节点/边列表 |
| 规则是不是被列在最前面 | 「可用规则 (优先沿规则推演)」段 |

---

## 三、节点"为什么会发生？"详细解释（P1-7）

### 3.1 它解决什么问题？

推演步骤卡片里每一步只有一句 ≤40 字的简短 `explanation`，看完仍然不知道：

- 这个结论的**依据**是什么？
- 它隐含了哪些**假设**？
- 有没有**反例**会推翻它？

本功能对任意推演节点请求 LLM 生成**三段式详细解释**，并缓存到该分支的持久化文件里，多次查看不重复花 token。

### 3.2 如何触发

1. 切换到目标分支（必须是已经完成保存的分支，**不能是正在运行的 live 推演**）
2. 在画布上找到推演节点（金色虚线连出 / 节点右上角带「预测 N」角标）
3. **右键**该节点
4. 上下文菜单中出现新条目（仅对 `source=predicted` 节点可见）：

```
⚡ 从此推演                    Forward
🔍 为什么会发生？              Explain    ← 本功能入口
─────
✎ 编辑节点
✕ 删除节点
```

5. 点击「🔍 为什么会发生？」→ 画布右下角弹出**浮动解释面板**

### 3.3 浮动面板

```
┌─ 🔍 库存吃紧                   event  78%  ─ △ × ─┐
│                                                    │
│ 📌 依据                                            │
│ 「主供应商断货」节点直接触发本步，规则 R3 缓冲库存│
│ 阈值在 3 天以内时强制提示，结合现有节点中…         │
│                                                    │
│ 💭 假设                                            │
│ 假设当前缓冲库存确实低于 3 天阈值；假设没有备选… │
│                                                    │
│ ⚠ 反例                                             │
│ 若上游有应急储备机制激活，或周边仓库可调拨，则…   │
│                                                    │
│                              [🔄 重新生成]         │
└────────────────────────────────────────────────────┘
```

| 元素 | 说明 |
|---|---|
| **节点标签 + 类型 + confidence** | 顶部 header 显示当前解释对象的元信息 |
| **缓存徽章** | 若内容来自缓存（不是本次 LLM 生成），右上角显示绿色「缓存」标签 |
| **顶部 △ 按钮** | 折叠/展开面板（仅保留 header，body 隐藏） |
| **顶部 × 按钮** | 关闭面板 |
| **拖拽** | 在 header 上按下鼠标可拖动整个面板到画布任意位置 |
| **🔄 重新生成** | 忽略缓存，强制让 LLM 重新生成（适用于换模型 / 内容不满意） |

### 3.4 多面板堆叠

- **同时最多打开 3 个面板**：超过则自动关闭最早的一个
- **同一节点重复触发**：先关闭旧面板再开新面板，等效"刷新位置"
- **错位摆放**：初始位置按打开顺序每个偏移 24px，避免完全重叠
- **z-index 递增**：晚开的盖在早开的上面

### 3.5 流式生成

- 调 LLM 时面板每段 body 先显示「生成中…」（斜体灰色）
- LLM 返回完成 → 三段同时填充（不是逐字流，是按"字段"分段）
- 失败 → 当前段显示红色「生成失败：{原因} [点击重试]」

### 3.6 缓存策略

- **写入时机**：LLM 生成完成后，立刻把三段内容写入该分支文件的 `dag.explanations[nodeId]`
- **命中时机**：再次右键同一节点 → 跳过 LLM 直接渲染（响应时间约几十毫秒）
- **强制重生**：点击「🔄 重新生成」绕过缓存（每次都调 LLM）

### 3.7 切换分支时

切换到其他分支会**自动关闭所有浮动解释面板**——避免 nodeId 在新分支不存在/语义错位。

---

## 四、典型工作流串联示例

**场景：评估"主供应商断货"事件的影响，把握程度只有 70%**

```
1. 主干上建好供应链图谱（包含 n_12=主供应商断货、n_5=缓冲库存规则 等）
2. 右键 n_12 → 「⚡ 从此推演 (Forward)」打开推演对话框
3. 展开「What-if 约束」→ 搜索「主供应商断货」→ 点击「概率」按钮
4. 拖动滑块到 0.70
5. 在「场景描述」里写：「假设断货 3 天，叠加旺季订单激增 30%」
6. 点击「开始推演」
7. 推演完成 → 对话面板里新出现金色推演消息
8. 点击消息头部「📜 prompt」按钮 → 弹窗确认：
   - 「What-if 约束」段是否包含 "先验概率 = 0.70 (probability)"
   - 「额外场景说明」段是否完整保留了你写的描述
9. 关闭弹窗 → 在画布上右键金色虚线连出的某个预测节点（如「紧急调拨启动」）
10. 选「🔍 为什么会发生？」→ 浮动面板生成三段：
    - 依据：上游断货 + 缓冲库存阈值规则触发
    - 假设：仓库可达性 + 物流时效
    - 反例：若启用 B 级备选供应商则可跳过本步
11. 觉得"假设"段太乐观 → 点「🔄 重新生成」让 LLM 重答一次
12. 多开几个面板对比不同步骤的解释 → 拖到合适位置堆叠
13. 决策评估完成后，按现有流程"提升回主干"（P1-4 实现后）
```

---

## 五、常见问题

### Q1：概率约束设了 0.5，推演结果好像没什么变化？

`probability=0.5` 等同于"完全不确定"——既不偏向必然也不偏向禁止，对下游的 `effectiveProbability` 不会产生明显折扣。建议用更两极的值（如 0.2 或 0.8）才能看到差异。

### Q2：「为什么会发生？」菜单项灰了 / 没出现？

检查目标节点是否为推演节点（金色虚线 + 「预测 N」角标）。只有 `source=predicted` 的节点才显示该菜单项。普通图谱节点（蓝/橙/绿/紫等纯色节点）不可用。

### Q3：解释面板提示「请先等待推演完成或切换到对应分支」？

当前画布处于以下状态时不能解释：

- **trunk 主干**：主干节点不是推演节点，没有上下文链路
- **live 推演中**：还没生成 scenarioId，无法定位
- **无活动分支**：尚未发起任何推演

切到一个已完成的非主干分支即可。

### Q4：原始 prompt 弹窗内容显示"该分支创建于早期版本"？

`rawPrompt` 字段是本功能上线后才开始写入的。**旧分支没有这个数据**，无法补救（重新发起一次推演会生成新分支并带有 prompt 快照）。

### Q5：解释缓存能清除吗？

直接编辑该分支的 JSON 文件 `~/.tuiyan/scenarios/sc_xxx.json`，删掉 `dag.explanations` 字段对应的 key。或者在面板内点「🔄 重新生成」直接覆盖。

### Q6：概率约束的具体计算公式是什么？

```
- 现有节点 X 的 effectiveProbability 初值 = probability（用户输入）
- 现有节点 X 的 cumulativeCredibility 初值 = probability
- 下游预测节点 Y（triggered_by 包含 X）的 effectiveProbability
  ≈ LLM_confidence(Y) × (1 - ∏(1 - effProb(parent)))
  其中 effProb(X) 已被替换为用户先验
```

所以一个 0.7 的先验会让下游所有引用它的预测节点 effProb **整体打约 0.7 的折扣**，与 LLM 自己估的 confidence 联合作用。

---

*文档版本：v1.0 · 基于 ROADMAP-v2 Phase 1 推演深化包*
