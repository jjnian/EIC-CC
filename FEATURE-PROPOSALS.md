# EIC-CC 推演平台 — 功能扩展建议书

> 编写日期：2026-05-23
> 基于对项目前后端源码、已有设计文档和 git 历史的完整分析

---

## 目录

1. [项目现状概览](#1-项目现状概览)
2. [高优先级功能（核心体验增强）](#2-高优先级功能核心体验增强)
3. [中优先级功能（协作与效率提升）](#3-中优先级功能协作与效率提升)
4. [低优先级功能（差异化竞争力）](#4-低优先级功能差异化竞争力)
5. [技术基建改进](#5-技术基建改进)
6. [功能优先级矩阵](#6-功能优先级矩阵)

---

## 1. 项目现状概览

EIC-CC 是一个基于本体图谱的 AI 因果推演系统，当前已实现的核心能力：

| 模块 | 已实现功能 |
|------|-----------|
| **图谱建模** | 自然语言对话建图、文档导入（PDF/DOCX/图片）、增量去重合并、撤销/重做（50步） |
| **因果推演** | 前向推演 / 溯因推演、多起点、What-if 约束（force/block）、累积置信度、剪枝报告 |
| **分支管理** | 多层嵌套分支、祖先链合并、分支对比、分支迁移 |
| **多模型支持** | 10个 LLM 提供商（Claude/GPT/DeepSeek/通义千问/Kimi/GLM 等）、请求级模型切换 |
| **图谱可视化** | SVG 画布、DAG 分层布局（LR/TB）、节点类型过滤、缩放平移、右键菜单 |
| **数据持久化** | 本地 JSON 文件存储（`~/.tuiyan/`）、原子写保证完整性 |
| **其他** | 推演模板、只读预览、导出 JSON、偏好设置 |

**技术栈：** Java 17 + Spring Boot 3（后端），Vue 3.5 + TypeScript 5.8 + Vite 6（前端），SSE 流式通信

---

## 2. 高优先级功能（核心体验增强）

### 2.1 图谱节点编辑与属性管理

**现状：** 当前 `NodeInfo.vue` 展示节点信息为只读状态，用户无法直接编辑节点的名称、类型、属性等字段。所有修改只能通过与 AI 对话间接完成。

**建议：**
- 双击节点进入内联编辑模式，可修改节点 label、type、描述
- 在 NodeInfo 面板的"属性" Tab 中支持增删改 props 键值对
- 支持拖拽改变节点之间的关系（新建边、删除边、修改边的 label）
- 右键菜单增加"编辑节点"、"删除节点"、"添加关系"选项
- 所有手动编辑操作纳入撤销/重做历史栈

**价值：** 让用户不完全依赖 AI 即可精细调整图谱，提升建模效率和准确性。

---

### 2.2 图谱搜索与定位

**现状：** 当图谱节点较多时（120+ 节点），用户难以快速找到目标节点，只能目视扫描或使用类型过滤。

**建议：**
- 画布顶部增加搜索框，支持按节点名称/类型/属性模糊搜索
- 搜索结果高亮匹配节点，非匹配节点半透明
- 点击搜索结果自动定位画布到目标节点（复用已有的 `focusNode` 方法）
- 支持快捷键 `Ctrl+F` 唤起搜索
- 搜索历史记录（最近 10 条）

**价值：** 大型图谱中的导航效率提升 5-10 倍。

---

### 2.3 推演结果可视化增强

**现状：** 推演结果通过 `ScenarioTimeline.vue` 以列表形式展示，置信度以数字显示。推演节点在画布上以金色虚线边框区分，但缺乏直观的概率热力图。

**建议：**
- **热力图模式：** 推演完成后可切换为热力图视图，节点颜色按有效概率从绿（高）到红（低）渐变
- **路径高亮：** 点击推演时间线中的某个步骤时，不仅聚焦到节点，还高亮该节点的整条因果链路径
- **动画回放：** 支持推演过程的逐步动画回放（按时间线步骤依次点亮节点）
- **概率标注：** 在推演节点上直接标注有效概率数值（可在偏好中开关）
- **关键路径标记：** 自动识别并高亮置信度最高的推演路径

**价值：** 将推演结果从"阅读列表"升级为"直观理解"，降低分析门槛。

---

### 2.4 对话上下文优化

**现状：** `ChatPanel.vue` 每次对话会将当前图谱全部 nodes/edges 作为上下文发送给 LLM（超过 120 节点时截断为 3-hop 邻域）。对话历史没有摘要机制。

**建议：**
- **图谱上下文摘要：** 后端生成图谱的结构化摘要（节点数、类型分布、核心路径）替代原始 JSON，减少 token 消耗
- **对话记忆压缩：** 当对话超过一定长度时，自动将早期消息压缩为摘要保留关键信息
- **选区对话：** 支持框选一组节点后发起对话，LLM 上下文仅包含选中节点及其 1-hop 邻域
- **上下文 token 计数显示：** 在聊天面板底部显示当前上下文的预估 token 数，帮助用户理解 AI 理解的范围

**价值：** 降低 API 调用成本，提升 AI 回答的精准度。

---

### 2.5 模型连接测试与健康检查

**现状：** `SettingsView.vue` 模型列表为只读展示，用户配置的 API Key / Base URL 是否可用只能在实际对话时才能验证。

**建议：**
- 模型列表每个卡片增加"测试连接"按钮，发送简单的 ping 请求验证 API 可达性
- 测试结果显示延迟（ms）和状态（成功/失败/超时）
- 设置页增加"全部测试"按钮，批量检测所有模型
- 首次进入设置页时自动检测默认模型的可用性
- 失败时展示友好的错误提示（API Key 无效、网络不通、余额不足等）

**价值：** 减少"配置了模型但发现不能用"的挫败感，提升首次使用体验。

---

## 3. 中优先级功能（协作与效率提升）

### 3.1 图谱版本历史

**现状：** 当前只有内存中的撤销/重做栈（50步），刷新页面即丢失。本体模型的修改通过防抖 1.2s 自动 PUT 到后端，但没有版本记录。

**建议：**
- 每次自动保存时记录一个版本快照（带时间戳和摘要，如"+3节点 +2关系"）
- 版本列表面板，显示最近 100 个版本
- 支持预览任意历史版本（只读模式）
- 支持回滚到任意历史版本（带确认弹窗）
- 支持两个版本之间的差异对比（新增/删除/修改的节点和关系）
- 后端存储策略：最近 24 小时每次保存记录，超过 24 小时按小时合并，超过 7 天按天合并

**价值：** 让用户无后顾之忧地大胆建模，误操作可随时回溯。

---

### 3.2 批量节点操作

**现状：** 画布上一次只能操作一个节点（拖拽移动、右键菜单）。

**建议：**
- 支持框选（按住 Shift 拖拽画出选区）多个节点
- 支持 Ctrl+点击 多选/取消选择
- 批量操作菜单：批量删除、批量修改类型、批量对齐（左对齐/垂直居中/等间距）
- 批量移动：拖拽任一选中节点，所有选中节点同步移动
- 复制粘贴：选中一组节点 Ctrl+C/V 复制到同一画布或另一个模型
- 选中节点统计：底部状态栏显示"已选 N 个节点，M 条关系"

**价值：** 大幅提升大型图谱的编辑效率。

---

### 3.3 推演对比增强

**现状：** `BranchCompareDialog.vue` 支持两个分支的文本列表对比，但只对比推演链中的节点，且不支持可视化差异。

**建议：**
- **可视化差异对比：** 在画布上叠加显示两个分支的推演结果，用不同颜色区分（蓝色=分支A独有，橙色=分支B独有，紫色=共同但概率不同）
- **多分支并排：** 支持同时打开 2-3 个分支，分屏并排显示各自的推演图谱
- **敏感性分析表格：** 以表格形式展示同一节点在不同分支/不同约束条件下的概率变化
- **最优分支推荐：** 根据用户定义的目标（如"最大化节点X的概率"或"最小化风险节点数"），自动推荐最优的 What-if 组合

**价值：** 从"看到差异"升级为"理解差异原因并做决策"。

---

### 3.4 图谱模板库

**现状：** 后端 `OntologyModelService` 首次启动时生成 3 个示例模型，但用户无法保存自定义模板。

**建议：**
- 支持将当前模型"另存为模板"（保留图谱结构，清除推演分支和对话历史）
- 内置模板库：供应链分析、风险评估、事故复盘、流程审计、组织架构等
- 模板分类和标签
- 从模板创建新模型时，自动填充描述和推荐的推演配置
- 模板导入/导出（JSON 文件）

**价值：** 降低新用户的学习曲线，形成知识资产复用。

---

### 3.5 国际化（i18n）

**现状：** 整个前端 UI 硬编码为中文和英文混合（按钮和标签以英文为主，部分提示中文）。

**建议：**
- 引入 `vue-i18n`，将所有 UI 文本抽取为翻译键
- 支持中文简体、英文两种语言（可后续扩展）
- 语言切换入口放在设置页
- 后端错误消息也支持国际化
- LLM 的 system prompt 根据语言设置调整

**价值：** 扩大潜在用户群体，为产品国际化做准备。

---

### 3.6 导出格式丰富化

**现状：** 仅支持导出 JSON 和复制图谱摘要到剪贴板。

**建议：**
- **导出为图片：** 将画布渲染为 PNG/SVG 文件（适合嵌入文档/PPT）
- **导出为 PDF 报告：** 包含图谱截图、节点列表、关系列表、推演结论和置信度
- **导出为 Markdown：** 结构化的文本报告，可粘贴到文档/Wiki
- **导出为 Mermaid/DOT 语法：** 可在其他工具中重建图谱
- **导出为 Excel：** 节点表 + 关系表 + 推演结果表，适合数据分析
- **一键生成汇报 PPT：** 自动组织推演过程和结论为幻灯片

**价值：** 打通推演结果到工作汇报的 last mile。

---

## 4. 低优先级功能（差异化竞争力）

### 4.1 实时协作（多人编辑）

**现状：** 单用户系统，无认证机制，本地文件存储。

**建议：**
- 引入 WebSocket 实现图谱的实时协作编辑
- 光标感知：显示其他用户正在编辑的节点
- 操作锁定：同一节点同时只能有一人编辑
- 冲突解决：基于 CRDT 或 OT 算法的自动合并
- 用户头像/颜色标识

**前置条件：** 需要先引入用户认证系统和数据库存储
**价值：** 从个人工具升级为团队工具。

---

### 4.2 图谱知识库与跨模型关联

**现状：** 每个本体模型是独立的，模型之间没有任何关联。

**建议：**
- **全局知识库：** 将所有模型的节点汇入统一索引，支持跨模型全文搜索
- **模型关联：** 允许在模型 A 中引用模型 B 的节点（创建跨模型边）
- **知识图谱合并：** 将多个模型合并为一个大图（自动去重同名节点）
- **知识推荐：** 建模时自动推荐其他模型中的相关节点/模式
- **标签和分类体系：** 为模型打标签，建立知识分类树

**价值：** 形成组织级知识图谱，打破信息孤岛。

---

### 4.3 自定义规则引擎

**现状：** 推演完全依赖 LLM 的推理能力，规则驱动（`rule_driven`）边在推演中优先考虑，但规则本身无法形式化定义。

**建议：**
- 允许用户为关系（边）定义条件规则（如"当节点A概率>0.7 且 节点B状态=active 时，触发节点C"）
- 规则编辑器（可视化 IF-THEN 界面）
- 推演时先执行规则引擎，再用 LLM 补充推理
- 规则冲突检测和优先级设定
- 规则执行日志和审计

**价值：** 让推演结果更可控、可解释、可审计，适合合规要求高的场景。

---

### 4.4 时间序列推演

**现状：** 推演是无时间维度的因果链推理。

**建议：**
- 为节点和关系增加时间属性（生效时间、失效时间、延迟时间）
- 推演时考虑时间序列：节点 A 在 T1 发生后，经过 delay=2d，节点 B 在 T3 发生
- 时间线视图：甘特图式的时间轴展示推演结果的时间分布
- 支持设置"推演截止时间"：只预测指定时间范围内可能发生的事件
- 历史数据对照：将推演结果与实际发生的事件对比

**价值：** 从"可能发生什么"升级为"什么时候可能发生"，支持更精确的预测和排期。

---

### 4.5 外部数据源集成

**现状：** 数据输入仅支持手动对话和文档上传。

**建议：**
- **API 数据源：** 对接外部 REST API，定时或手动拉取数据更新图谱节点属性
- **数据库连接：** 支持连接 MySQL/PostgreSQL，将表结构映射为本体节点
- **知识图谱导入：** 支持导入 RDF/OWL/TTL 格式的标准本体
- **Webhook 触发：** 外部系统通过 Webhook 触发图谱更新或推演
- **CSV/Excel 批量导入：** 通过表格批量创建节点和关系

**价值：** 让推演系统不再是数据孤岛，与企业现有系统打通。

---

### 4.6 推演评估与校准

**现状：** 推演结果的置信度完全由 LLM 给出，没有校准机制。

**建议：**
- **结果反馈机制：** 用户可标记推演节点为"实际发生"或"未发生"
- **准确率统计：** 按模型/推演方向/领域统计历史推演准确率
- **置信度校准：** 基于历史反馈数据，对 LLM 给出的置信度进行贝叶斯校准
- **模型对比实验：** 同一推演配置使用不同 LLM，自动对比各模型的推演质量
- **推演日志：** 记录每次推演的完整参数和结果，便于事后审计

**价值：** 从"AI说可能"升级为"基于历史数据，AI预测准确率约 78%"，建立用户信任。

---

### 4.7 移动端适配

**现状：** 纯桌面端设计，SVG 画布交互（拖拽、右键菜单、Ctrl+滚轮缩放）不适合触屏。

**建议：**
- 响应式布局：小屏幕时聊天面板全屏，图谱和信息面板可切换
- 触屏手势：双指缩放、单指平移、长按代替右键
- 移动端精简模式：只保留查看和简单对话功能，复杂编辑引导到桌面端
- PWA 支持：可添加到手机桌面，离线查看已加载的模型

**价值：** 覆盖移动办公场景，随时查看和讨论推演结果。

---

## 5. 技术基建改进

### 5.1 数据库存储迁移

**现状：** 所有数据以 JSON 文件存储在 `~/.tuiyan/` 目录。

**建议：**
- 引入 SQLite（单机）或 PostgreSQL（多用户）作为数据存储
- 使用 Spring Data JPA 管理数据模型
- 图谱数据使用 JSONB 字段存储（兼顾灵活性和查询性能）
- 保留 JSON 文件导入/导出能力作为数据迁移工具
- 增加数据库备份和恢复功能

**价值：** 为多用户、高并发、全文搜索等高级功能提供基础。

---

### 5.2 用户认证与权限

**现状：** 无认证机制，任何人可访问所有数据。

**建议：**
- 支持用户名/密码登录（内置用户管理）
- 支持 SSO 集成（OAuth2 / OIDC / LDAP）
- 模型级别的权限控制：所有者、编辑者、查看者
- 操作审计日志：谁在什么时间做了什么操作

**价值：** 企业部署的基本前提。

---

### 5.3 自动化测试

**现状：** 前后端均无自动化测试。

**建议：**
- **后端：** JUnit 5 + Mockito 单元测试，覆盖 Service 层核心逻辑（推演概率计算、ID 重写、文件解析）
- **后端集成测试：** Spring Boot Test + MockMvc，覆盖所有 REST API
- **前端：** Vitest 单元测试，覆盖 composables 中的纯逻辑函数
- **前端组件测试：** Vue Test Utils，覆盖关键交互流程
- **E2E 测试：** Playwright 覆盖 5 条主路径（欢迎页建图、对话建模、推演、分支切换、文档导入）
- CI 集成：GitHub Actions 在 PR 时自动运行测试

**价值：** 保障重构和新功能开发的安全性，减少回归 bug。

---

### 5.4 性能优化

**现状：** 大型图谱（120+ 节点）可能出现渲染卡顿，SVG 全量渲染无虚拟化。

**建议：**
- **画布虚拟化：** 只渲染视口内可见的节点和边
- **图谱数据索引：** 后端为节点/关系建立内存索引，加速邻域查询
- **增量渲染：** 节点变更时只更新受影响的 SVG 元素，而非全量重绘
- **Web Worker 布局计算：** 将 `autoLayout` 的分层排序算法移至 Web Worker，避免阻塞主线程
- **SSE 背压控制：** 推演步骤推送频率过快时自动降速
- **懒加载：** 模型列表分页加载，对话历史按需加载

**价值：** 支撑 500+ 节点的大型图谱流畅操作。

---

### 5.5 日志与监控

**现状：** 后端仅有基本的控制台日志（`SLF4J`），无结构化监控。

**建议：**
- 结构化日志：JSON 格式日志，包含 requestId、userId、modelId 等上下文
- LLM 调用监控：记录每次调用的模型、token 用量、延迟、成功率
- 前端错误监控：接入 Sentry 或自建错误收集
- 健康检查端点：`/actuator/health` 检查文件系统可用性和 LLM 连通性
- 仪表盘：LLM 调用次数趋势、token 消耗统计、推演成功率

**价值：** 生产环境运维的必要基础。

---

### 5.6 容器化部署

**现状：** 通过 `start.sh` 手动启动前后端。

**建议：**
- Dockerfile（多阶段构建：Java + Node.js 构建 → JRE 运行时）
- docker-compose.yml（一键启动前后端 + 可选数据库）
- 环境变量配置（LLM API Key、端口、数据目录等）
- 持久化卷映射（`~/.tuiyan/` → Docker volume）
- Kubernetes Helm Chart（适用于企业级部署）

**价值：** 一行命令部署，降低运维门槛。

---

## 6. 功能优先级矩阵

| 功能 | 开发难度 | 用户价值 | 前置依赖 | 建议排期 |
|------|---------|---------|---------|---------|
| 2.1 节点编辑与属性管理 | ⭐⭐ | ⭐⭐⭐⭐⭐ | 无 | **第一期** |
| 2.2 图谱搜索与定位 | ⭐ | ⭐⭐⭐⭐ | 无 | **第一期** |
| 2.5 模型连接测试 | ⭐ | ⭐⭐⭐⭐ | 无 | **第一期** |
| 2.3 推演可视化增强 | ⭐⭐⭐ | ⭐⭐⭐⭐ | 无 | **第一期** |
| 2.4 对话上下文优化 | ⭐⭐ | ⭐⭐⭐ | 无 | **第一期** |
| 5.3 自动化测试 | ⭐⭐⭐ | ⭐⭐⭐⭐ | 无 | **第一期** |
| 5.6 容器化部署 | ⭐⭐ | ⭐⭐⭐⭐ | 无 | **第一期** |
| 3.1 版本历史 | ⭐⭐⭐ | ⭐⭐⭐⭐ | 无 | **第二期** |
| 3.2 批量节点操作 | ⭐⭐ | ⭐⭐⭐ | 2.1 节点编辑 | **第二期** |
| 3.3 推演对比增强 | ⭐⭐⭐ | ⭐⭐⭐ | 无 | **第二期** |
| 3.4 图谱模板库 | ⭐⭐ | ⭐⭐⭐ | 无 | **第二期** |
| 3.6 导出格式丰富化 | ⭐⭐ | ⭐⭐⭐ | 无 | **第二期** |
| 5.4 性能优化 | ⭐⭐⭐ | ⭐⭐⭐ | 无 | **第二期** |
| 5.5 日志与监控 | ⭐⭐ | ⭐⭐⭐ | 无 | **第二期** |
| 3.5 国际化 | ⭐⭐⭐ | ⭐⭐ | 无 | **第三期** |
| 5.1 数据库迁移 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 无 | **第三期** |
| 5.2 用户认证 | ⭐⭐⭐ | ⭐⭐⭐ | 5.1 数据库 | **第三期** |
| 4.1 实时协作 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 5.1 + 5.2 | **第三期** |
| 4.2 跨模型关联 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 5.1 数据库 | **第三期** |
| 4.3 规则引擎 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 无 | **第三期** |
| 4.4 时间序列推演 | ⭐⭐⭐⭐ | ⭐⭐ | 无 | **远期** |
| 4.5 外部数据源集成 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 5.1 数据库 | **远期** |
| 4.6 推演评估校准 | ⭐⭐⭐ | ⭐⭐⭐ | 5.1 数据库 | **远期** |
| 4.7 移动端适配 | ⭐⭐⭐⭐ | ⭐⭐ | 无 | **远期** |

> **难度说明：** ⭐ = 1-2天，⭐⭐ = 3-5天，⭐⭐⭐ = 1-2周，⭐⭐⭐⭐ = 2-4周，⭐⭐⭐⭐⭐ = 1月+

---

## 附录：建议的第一期开发顺序

1. **图谱搜索与定位**（快速见效，1-2天）
2. **模型连接测试**（提升首次体验，1-2天）
3. **节点编辑与属性管理**（核心功能补全，3-5天）
4. **对话上下文优化**（降本增效，3-5天）
5. **推演可视化增强**（体验升级，1-2周）
6. **容器化部署**（降低部署门槛，3-5天）
7. **自动化测试**（保障后续开发质量，1-2周）

以上 7 个功能预计总工期约 4-6 周，可显著提升产品的核心使用体验。
